using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParticleDestroying : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		/*if (ParticleSpawn.istouch == true) {
			//Invoke ("Destroy", 2.0f);
			ParticleSpawn.istouch = false;
		}*/
	}
	/*void Destroy()
	{
		ParticleSpawn.particle.SetActive (false);
	}*/
}
